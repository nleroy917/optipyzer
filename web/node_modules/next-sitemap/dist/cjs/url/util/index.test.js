"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const index_1 = require("./index");
describe('next-sitemap', () => {
    test('isURL : Valid', () => {
        expect(index_1.isURL('https://example.com')).toBeTruthy();
    });
    test('isURL : Invalid', () => {
        expect(index_1.isURL('/someone-relative/path/item.jpg')).toBeFalsy();
    });
    test('cleanPath : Relative Path', () => {
        expect(index_1.cleanPath('./epic///awesome///path')).toBe('./epic/awesome/path');
    });
    test('cleanPath: Public Url', () => {
        expect(index_1.cleanPath('https://www.example.com//epic///awesome///path')).toBe('https://www.example.com/epic/awesome/path');
    });
    test('generateUrl: with relative slug', () => {
        const url = index_1.generateUrl('https://base.example.com', '//awesome/path');
        expect(url).toBe('https://base.example.com/awesome/path');
    });
    test('generateUrl: with external slug', () => {
        const url = index_1.generateUrl('https://base.example.com', 'https://cdn.another.site/new//path');
        expect(url).toBe('https://cdn.another.site/new/path');
    });
    test('isNextInternalUrl', () => {
        expect(index_1.isNextInternalUrl('/_app')).toBeTruthy();
        expect(index_1.isNextInternalUrl('/404')).toBeTruthy();
        expect(index_1.isNextInternalUrl('/_random')).toBeTruthy();
    });
    test('isNextInternalUrl: url params', () => {
        expect(index_1.isNextInternalUrl('/[id]')).toBeTruthy();
        expect(index_1.isNextInternalUrl('/blog/[id]')).toBeTruthy();
    });
    test('isNextInternalUrl: allow urls with underscore`', () => {
        expect(index_1.isNextInternalUrl('/_some_url')).toBeTruthy();
        expect(index_1.isNextInternalUrl('/some_url/[param]')).toBeTruthy();
        expect(index_1.isNextInternalUrl('/some_url')).toBeFalsy();
        expect(index_1.isNextInternalUrl('/some-404')).toBeFalsy();
    });
});
